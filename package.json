{
	"name": "propagate-bans",
	"version": "1.0.0",
	"author": "epsilon",
	"description": "Propogates bans to all connected servers",
	"license": "MIT",
	"main": "dist/index.js",
	"dependencies": {
		"fs": "^0.0.1-security"
	},
	"devDependencies": {
		"@types/node": "^14.14.31"
	},
	"scripts": {
		"compile": "rm -rf dist && tsc",
		"watch": "tsc -w",
		"run": "npm i && node ."
	}
}
